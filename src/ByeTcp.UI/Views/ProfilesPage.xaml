<Page
    x:Class="ByeTcp.UI.Views.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ByeTcp.UI.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16">
            <TextBlock Text="Profiles" Style="{StaticResource TitleLargeTextBlock}" />
            <Button Content="Create New" Icon="Add" HorizontalAlignment="Right" />
        </StackPanel>

        <!-- Profiles List -->
        <GridView 
            Grid.Row="1"
            ItemsSource="{Binding Profiles}"
            SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
            Margin="0,16,0,0"
            SelectionMode="Single">
            
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Border 
                        Width="280" 
                        Height="180"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                        <StackPanel Spacing="8">
                            <TextBlock 
                                Text="{Binding Name}" 
                                Style="{StaticResource SubtitleTextBlock}"
                                TextWrapping="Wrap" />
                            <TextBlock 
                                Text="{Binding Description}" 
                                Style="{StaticResource CaptionTextBlockStyle}"
                                TextWrapping="Wrap"
                                Height="40" />
                            
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                <Badge 
                                    Content="{Binding CongestionProvider}"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}" />
                                <Badge 
                                    Content="ACK: {Binding TcpAckFrequency}"
                                    Background="{ThemeResource SystemFillColorSuccessBrush}" />
                            </StackPanel>
                            
                            <Border 
                                Visibility="{Binding IsReadOnly, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="{ThemeResource SystemFillColorAttentionBrush}"
                                Padding="4,2"
                                CornerRadius="4"
                                HorizontalAlignment="Left">
                                <TextBlock Text="Read-only" Style="{StaticResource CaptionTextBlockStyle}" />
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,16,0,0" HorizontalAlignment="Right">
            <Button Content="Preview" Command="{Binding PreviewProfileCommand}" />
            <Button Content="Apply" Command="{Binding ApplyProfileCommand}" Style="{StaticResource AccentButtonStyle}" />
            <Button Content="Edit" Command="{Binding StartEditCommand}" />
            <Button Content="Clone" />
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid 
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            Opacity="0.8"
            Grid.RowSpan="3">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <ProgressRing IsActive="True" />
                <TextBlock Text="Loading profiles..." Style="{StaticResource BodyTextBlockStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
